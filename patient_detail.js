function showBox(box)
{
    var boxToShow = document.getElementById(box);
    boxToShow.style.display = 'block';
}
function clearBox(box)
{
    var boxToClear = document.getElementById(box);
    boxToClear.style.display = 'none';
}
